export interface Translations {
  language: string;
  navigationLabel: string;
  theme: string;
  themeDark: string;
  themeLight: string;
  results: string;
  noResults: string;
  currentGitHash: string;
}
